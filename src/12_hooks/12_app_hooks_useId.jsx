import React from "react";

const App = () => {
  const list = ["a", "b", "c", "d", "e"];
  return (
    <div>
      <ul>
        {list.map((item, index) => {
          
        })}
      </ul>
    </div>
  );
};

export default App;
